# Why Orb-produced WebAssembly is smaller than Rust

C | Rust | Zig | Orb

- Orb doesn’t need a complex allocator, a simple bump allocator is fine.

## When Rust is better

- Rust has a much more powerful optimizer.